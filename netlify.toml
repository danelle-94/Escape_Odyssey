[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# API Redirect to Netlify Serverless Function
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200

# SPA Routing Fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
